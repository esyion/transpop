/**
 * 应用外壳 feature 的对外入口（全局布局辅助组件与启动引导）。
 */
export { CommandPalette } from "./components/command-palette";
export type { CommandAction } from "./components/command-palette";
export { useAppBootstrap } from "./hooks/use-app-bootstrap";
